git clone https://github.com/ian-knight-uofa/git-practice-03.git

cd git-practice-03/

git branch branch1
git checkout branch1

rm -rf dir1/dir2
mv dir1/foo .
rm -rf dir3/bar_copy
touch newfile1

git add .
git commit -m "Modify files to match configuration for branch1"

git checkout main

git branch branch2
git checkout branch2

rm -rf dir1/foo
mv dir1/dir2/foo_modified dir1/
mkdir dir1/dir3
touch dir1/dir3/newfile2

git add .
git commit -m "Modify files to match configuration for branch2"

ls -R

git branch -D branch3
