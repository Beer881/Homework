cd git-practice-04

git branch -r

git checkout main
git merge origin/ready-* -m "merge ready-* branches into main"

git push origin --delete $(git branch -r | grep ready | sed 's/origin\///')

git checkout main
git pull
git branch -r | grep update | sed 's/origin\///' | xargs -I {} git checkout {} && git merge origin/main -m "merge main into {}"
git push origin --all

